import { GalleryElementDirective } from './gallery-element.directive';

describe('GalleryElementDirective', () => {
  it('should create an instance', () => {
    const directive = new GalleryElementDirective();
    expect(directive).toBeTruthy();
  });
});
